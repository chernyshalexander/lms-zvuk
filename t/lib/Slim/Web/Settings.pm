package Slim::Web::Settings;

use strict;
use warnings;

# Minimal mock for isolation tests. Real module is the base class for
# plugin web settings pages; Settings.pm only needs this to exist as a
# base class to load successfully.

1;
